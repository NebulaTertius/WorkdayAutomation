CREATE PROCEDURE [AI].[MappingValidations]
AS

BEGIN TRY BEGIN TRANSACTION 
	SELECT 1
IF ((SELECT XACT_STATE()) = 1) COMMIT TRANSACTION END TRY 
BEGIN CATCH IF ((SELECT XACT_STATE()) = -1) ROLLBACK TRANSACTION END CATCH
